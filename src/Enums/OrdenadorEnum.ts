import IOrdenadorEnum from 'interfaces/IOrdenadorEnum';

const OrdenadorEnum: IOrdenadorEnum = Object.freeze({
    porcao: 'size',
    qtd_pessoas: 'serving',
    preco: 'price',
});

export default OrdenadorEnum;
